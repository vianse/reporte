<div class="container">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">Iniciar Sesión</h3>
        </div>
          <div class="panel-body">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                <fieldset>
                <div class="form-group">
                  <%= f.email_field :email, autofocus: true, :class => "form-control",:placeholder => "ejemplo@example.com" %>
              </div>
              <div class="form-group">
                 <%= f.password_field :password, autocomplete: "off", :class  => "form-control",  :placeholder =>"Password" %>
              </div>
               <% if devise_mapping.rememberable? -%>
              <div class="checkbox">
                  <label>
                    <%= f.check_box :remember_me %>
                    Recordar cuenta
                  </label>
                </div>
              <% end %>
              <div class="actions">
                 <%= f.submit "Log in", :class => "btn btn-lg btn-success btn-block" %>
              </div>
            </fieldset>

            <% end -%>
              <hr/>
              <center><h4>O</h4></center>
              <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
                <%= link_to "Registrar", new_registration_path(resource_name), :class => "btn btn-lg btn-info btn-block" %><br />
              <% end -%>
              <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
                <%= link_to "Recuperar password?", new_password_path(resource_name) %><br />
              <% end -%>

           
          </div>
      </div>
    </div>
  </div>
</div>

