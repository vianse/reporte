  <!-- begin navbar-right -->
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown navbar-user">
                        <a href="javascript:;" class="dropdown-toggle" data-toggle="dropdown">
                            <span class="hidden-xs"><%= current_sistema.email %></span> <b class="caret"></b>
                        </a>
                        <ul class="dropdown-menu pull-right">
                            <li><a href="/sistemas/edit">Seguridad</a></li>
                            <li class="divider"></li>
                            <li><%= link_to "Cerrar sesión", destroy_sistema_session_path, method: :delete  %></li>
                        </ul>
                    </li>
                </ul>
<p id="notice"><%= notice %></p>

<h1>Listing Payments</h1>

<table>
  <thead>
    <tr>
      <th>App</th>
      <th>Importe</th>
      <th>Fecha</th>
      <th>Dias</th>
      <th>Estatus</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @payments.each do |payment| %>
      <tr>
        <td><%= payment.app_id %></td>
        <td><%= payment.importe %></td>
        <td><%= payment.fecha %></td>
        <td><%= payment.dias %></td>
        <td><%= payment.estatus %></td>
        <td><%= link_to 'Show', payment %></td>
        <td><%= link_to 'Edit', edit_payment_path(payment) %></td>
        <td><%= link_to 'Destroy', payment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Payment', new_payment_path %>
